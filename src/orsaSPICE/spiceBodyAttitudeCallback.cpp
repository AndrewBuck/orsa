#include <orsaSPICE/spiceBodyAttitudeCallback.h>

#include <orsaSPICE/spice.h>

using namespace orsa;
using namespace orsaSPICE;

/* 
   SpiceBodyAttitudeCallback::SpiceBodyAttitudeCallback() : BodyAttitudeCallback() { 
   // dummyMatrix = orsa::Matrix::identity();
   }
   
   orsa::Matrix SpiceBodyAttitudeCallback::localToGlobal(const orsa::Time & t) const {
   // ORSA_DEBUG("using BodyAttitudeCallback...");
   return SPICE::instance()->localToGlobal(_name.getRef(), t);
   }
   
   orsa::Matrix SpiceBodyAttitudeCallback::globalToLocal(const orsa::Time & t) const {
   // ORSA_DEBUG("using BodyAttitudeCallback...");
   return SPICE::instance()->globalToLocal(_name.getRef(), t);
   }
*/
